# Open Payment and NPES Data Merge Project

This project creates a database of Open Payment records that can be searched using an NPI number.

```
$ pyspark --master local[*]
```

## Requirements
* Install Apache Spark
* Install Python3
* Install PostgreSQL
* Elastic Search
